import svg from "../../a2k-icons.svg";

export default svg;

// run in the node environment
// import the node module
// parse the tree
// get all symbol elements. get the id for each symbol
// output it into a typescript file that exports the names
// run this as part of the build scripts? when is it best for this script to run?
